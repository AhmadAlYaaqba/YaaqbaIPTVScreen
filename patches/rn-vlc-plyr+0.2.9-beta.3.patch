diff --git a/node_modules/rn-vlc-plyr/android/src/main/java/com/rnvlcplyr/RnVlcPlyrView.kt b/node_modules/rn-vlc-plyr/android/src/main/java/com/rnvlcplyr/RnVlcPlyrView.kt
index 4e4eaf4..f6d1ff4 100644
--- a/node_modules/rn-vlc-plyr/android/src/main/java/com/rnvlcplyr/RnVlcPlyrView.kt
+++ b/node_modules/rn-vlc-plyr/android/src/main/java/com/rnvlcplyr/RnVlcPlyrView.kt
@@ -18,12 +18,19 @@ class RnVlcPlyrView : FrameLayout {
 
     private var lastVolumeBeforeMute: Int = 100
     private var isEnded = false
+    private var hasEmittedLoad = false
 
     private var url: String? = null
     private var autoPlay: Boolean = true
     private var loop: Boolean = false
     private var muted: Boolean = false
 
+    // Callbacks to bridge to Fabric event emitter
+    var onProgressCallback: ((currentTime: Double, duration: Double) -> Unit)? = null
+    var onLoadCallback: ((duration: Double, width: Double, height: Double) -> Unit)? = null
+    var onStateChangeCallback: ((state: String) -> Unit)? = null
+    var onErrorCallback: ((message: String, code: Int) -> Unit)? = null
+
     constructor(context: Context) : super(context) {
         init(context)
     }
@@ -46,8 +53,45 @@ class RnVlcPlyrView : FrameLayout {
         mediaPlayer?.attachViews(videoLayout!!, null, false, false)
 
         mediaPlayer?.setEventListener { event ->
-            if (event.type == MediaPlayer.Event.EndReached) {
-                isEnded = true
+            when (event.type) {
+                MediaPlayer.Event.TimeChanged -> {
+                    val player = mediaPlayer ?: return@setEventListener
+                    val currentTimeMs = player.time.toDouble()
+                    val durationMs = player.length.toDouble()
+                    val currentTimeSec = currentTimeMs / 1000.0
+                    val durationSec = durationMs / 1000.0
+
+                    // Emit onLoad on first valid duration
+                    if (!hasEmittedLoad && durationSec > 0) {
+                        hasEmittedLoad = true
+                        onLoadCallback?.invoke(durationSec, 0.0, 0.0)
+                    }
+
+                    // Emit onProgress
+                    if (durationSec > 0) {
+                        onProgressCallback?.invoke(currentTimeSec, durationSec)
+                    }
+                }
+                MediaPlayer.Event.Playing -> {
+                    onStateChangeCallback?.invoke("playing")
+                }
+                MediaPlayer.Event.Paused -> {
+                    onStateChangeCallback?.invoke("paused")
+                }
+                MediaPlayer.Event.Stopped -> {
+                    onStateChangeCallback?.invoke("stopped")
+                }
+                MediaPlayer.Event.Buffering -> {
+                    onStateChangeCallback?.invoke("buffering")
+                }
+                MediaPlayer.Event.EncounteredError -> {
+                    onStateChangeCallback?.invoke("error")
+                    onErrorCallback?.invoke("VLC playback error", -1)
+                }
+                MediaPlayer.Event.EndReached -> {
+                    isEnded = true
+                    onStateChangeCallback?.invoke("ended")
+                }
             }
         }
     }
@@ -58,6 +102,7 @@ class RnVlcPlyrView : FrameLayout {
         url = newUrl
 
         isEnded = false
+        hasEmittedLoad = false
 
         prepareMedia()
     }
diff --git a/node_modules/rn-vlc-plyr/android/src/main/java/com/rnvlcplyr/RnVlcPlyrViewManager.kt b/node_modules/rn-vlc-plyr/android/src/main/java/com/rnvlcplyr/RnVlcPlyrViewManager.kt
index fef4043..ee325ee 100644
--- a/node_modules/rn-vlc-plyr/android/src/main/java/com/rnvlcplyr/RnVlcPlyrViewManager.kt
+++ b/node_modules/rn-vlc-plyr/android/src/main/java/com/rnvlcplyr/RnVlcPlyrViewManager.kt
@@ -1,13 +1,17 @@
 package com.rnvlcplyr
 
 import android.graphics.Color
+import com.facebook.react.bridge.Arguments
+import com.facebook.react.bridge.ReactContext
 import com.facebook.react.module.annotations.ReactModule
 import com.facebook.react.uimanager.SimpleViewManager
 import com.facebook.react.uimanager.ThemedReactContext
 import com.facebook.react.uimanager.ViewManagerDelegate
 import com.facebook.react.uimanager.annotations.ReactProp
+import com.facebook.react.uimanager.events.EventDispatcher
 import com.facebook.react.viewmanagers.RnVlcPlyrViewManagerInterface
 import com.facebook.react.viewmanagers.RnVlcPlyrViewManagerDelegate
+import com.facebook.react.uimanager.UIManagerHelper
 
 @ReactModule(name = RnVlcPlyrViewManager.NAME)
 class RnVlcPlyrViewManager : SimpleViewManager<RnVlcPlyrView>(), RnVlcPlyrViewManagerInterface<RnVlcPlyrView> {
@@ -25,8 +29,54 @@ class RnVlcPlyrViewManager : SimpleViewManager<RnVlcPlyrView>(), RnVlcPlyrViewMa
     return NAME
   }
 
+  override fun getExportedCustomDirectEventTypeConstants(): MutableMap<String, Any>? {
+    return mutableMapOf(
+      "topStateChange" to mutableMapOf("registrationName" to "onStateChange"),
+      "topProgress" to mutableMapOf("registrationName" to "onProgress"),
+      "topLoad" to mutableMapOf("registrationName" to "onLoad"),
+      "topError" to mutableMapOf("registrationName" to "onError"),
+      "topVolumeChange" to mutableMapOf("registrationName" to "onVolumeChange")
+    )
+  }
+
   public override fun createViewInstance(context: ThemedReactContext): RnVlcPlyrView {
-    return RnVlcPlyrView(context)
+    val view = RnVlcPlyrView(context)
+    setupEventCallbacks(view, context)
+    return view
+  }
+
+  private fun setupEventCallbacks(view: RnVlcPlyrView, context: ThemedReactContext) {
+    view.onProgressCallback = { currentTime, duration ->
+      val event = Arguments.createMap().apply {
+        putDouble("currentTime", currentTime)
+        putDouble("duration", duration)
+      }
+      val dispatcher = UIManagerHelper.getEventDispatcherForReactTag(context, view.id)
+      dispatcher?.dispatchEvent(
+        TopProgressEvent(UIManagerHelper.getSurfaceId(context), view.id, currentTime, duration)
+      )
+    }
+
+    view.onLoadCallback = { duration, width, height ->
+      val dispatcher = UIManagerHelper.getEventDispatcherForReactTag(context, view.id)
+      dispatcher?.dispatchEvent(
+        TopLoadEvent(UIManagerHelper.getSurfaceId(context), view.id, duration, width, height)
+      )
+    }
+
+    view.onStateChangeCallback = { state ->
+      val dispatcher = UIManagerHelper.getEventDispatcherForReactTag(context, view.id)
+      dispatcher?.dispatchEvent(
+        TopStateChangeEvent(UIManagerHelper.getSurfaceId(context), view.id, state)
+      )
+    }
+
+    view.onErrorCallback = { message, code ->
+      val dispatcher = UIManagerHelper.getEventDispatcherForReactTag(context, view.id)
+      dispatcher?.dispatchEvent(
+        TopErrorEvent(UIManagerHelper.getSurfaceId(context), view.id, message, code)
+      )
+    }
   }
 
   @ReactProp(name = "url")
@@ -79,3 +129,65 @@ class RnVlcPlyrViewManager : SimpleViewManager<RnVlcPlyrView>(), RnVlcPlyrViewMa
     const val NAME = "RnVlcPlyrView"
   }
 }
+
+// --- Custom Fabric Events ---
+
+private class TopProgressEvent(
+  surfaceId: Int,
+  viewTag: Int,
+  private val currentTime: Double,
+  private val duration: Double
+) : com.facebook.react.uimanager.events.Event<TopProgressEvent>(surfaceId, viewTag) {
+  override fun getEventName() = "topProgress"
+  override fun getEventData(): com.facebook.react.bridge.WritableMap {
+    return Arguments.createMap().apply {
+      putDouble("currentTime", currentTime)
+      putDouble("duration", duration)
+    }
+  }
+}
+
+private class TopLoadEvent(
+  surfaceId: Int,
+  viewTag: Int,
+  private val duration: Double,
+  private val width: Double,
+  private val height: Double
+) : com.facebook.react.uimanager.events.Event<TopLoadEvent>(surfaceId, viewTag) {
+  override fun getEventName() = "topLoad"
+  override fun getEventData(): com.facebook.react.bridge.WritableMap {
+    return Arguments.createMap().apply {
+      putDouble("duration", duration)
+      putDouble("width", width)
+      putDouble("height", height)
+    }
+  }
+}
+
+private class TopStateChangeEvent(
+  surfaceId: Int,
+  viewTag: Int,
+  private val state: String
+) : com.facebook.react.uimanager.events.Event<TopStateChangeEvent>(surfaceId, viewTag) {
+  override fun getEventName() = "topStateChange"
+  override fun getEventData(): com.facebook.react.bridge.WritableMap {
+    return Arguments.createMap().apply {
+      putString("state", state)
+    }
+  }
+}
+
+private class TopErrorEvent(
+  surfaceId: Int,
+  viewTag: Int,
+  private val message: String,
+  private val code: Int
+) : com.facebook.react.uimanager.events.Event<TopErrorEvent>(surfaceId, viewTag) {
+  override fun getEventName() = "topError"
+  override fun getEventData(): com.facebook.react.bridge.WritableMap {
+    return Arguments.createMap().apply {
+      putString("message", message)
+      putInt("code", code)
+    }
+  }
+}
diff --git a/node_modules/rn-vlc-plyr/ios/RnVlcPlyrView.mm b/node_modules/rn-vlc-plyr/ios/RnVlcPlyrView.mm
index fc80b89..8ee9103 100644
--- a/node_modules/rn-vlc-plyr/ios/RnVlcPlyrView.mm
+++ b/node_modules/rn-vlc-plyr/ios/RnVlcPlyrView.mm
@@ -23,6 +23,13 @@ - (void)stop;
 - (void)seek:(double)timeMs;
 - (void)setVolume:(double)volume;
 
+// callbacks
+@optional
+- (void)setOnProgressCallback:(void (^)(double currentTime, double duration))callback;
+- (void)setOnLoadCallback:(void (^)(double duration, double width, double height))callback;
+- (void)setOnStateChangeCallback:(void (^)(NSString *state))callback;
+- (void)setOnErrorCallback:(void (^)(NSString *message, NSInteger code))callback;
+
 @end
 
 using namespace facebook::react;
@@ -58,6 +65,53 @@ - (instancetype)initWithFrame:(CGRect)frame
       self.vlcPlayerView.autoresizingMask = UIViewAutoresizingFlexibleWidth | UIViewAutoresizingFlexibleHeight;
       self.contentView = self.vlcPlayerView;
 
+      // Wire up event callbacks from Swift -> Fabric event emitter
+      __weak RnVlcPlyrView *weakSelf = self;
+
+      if ([playerViewInstance respondsToSelector:@selector(setOnProgressCallback:)]) {
+        [(id)playerViewInstance setOnProgressCallback:^(double currentTime, double duration) {
+          RnVlcPlyrView *strongSelf = weakSelf;
+          if (strongSelf && strongSelf->_eventEmitter) {
+            auto emitter = std::static_pointer_cast<const RnVlcPlyrViewEventEmitter>(strongSelf->_eventEmitter);
+            RnVlcPlyrViewEventEmitter::OnProgress event{currentTime, duration};
+            emitter->onProgress(event);
+          }
+        }];
+      }
+
+      if ([playerViewInstance respondsToSelector:@selector(setOnLoadCallback:)]) {
+        [(id)playerViewInstance setOnLoadCallback:^(double duration, double width, double height) {
+          RnVlcPlyrView *strongSelf = weakSelf;
+          if (strongSelf && strongSelf->_eventEmitter) {
+            auto emitter = std::static_pointer_cast<const RnVlcPlyrViewEventEmitter>(strongSelf->_eventEmitter);
+            RnVlcPlyrViewEventEmitter::OnLoad event{duration, width, height};
+            emitter->onLoad(event);
+          }
+        }];
+      }
+
+      if ([playerViewInstance respondsToSelector:@selector(setOnStateChangeCallback:)]) {
+        [(id)playerViewInstance setOnStateChangeCallback:^(NSString *state) {
+          RnVlcPlyrView *strongSelf = weakSelf;
+          if (strongSelf && strongSelf->_eventEmitter) {
+            auto emitter = std::static_pointer_cast<const RnVlcPlyrViewEventEmitter>(strongSelf->_eventEmitter);
+            RnVlcPlyrViewEventEmitter::OnStateChange event{std::string([state UTF8String])};
+            emitter->onStateChange(event);
+          }
+        }];
+      }
+
+      if ([playerViewInstance respondsToSelector:@selector(setOnErrorCallback:)]) {
+        [(id)playerViewInstance setOnErrorCallback:^(NSString *message, NSInteger code) {
+          RnVlcPlyrView *strongSelf = weakSelf;
+          if (strongSelf && strongSelf->_eventEmitter) {
+            auto emitter = std::static_pointer_cast<const RnVlcPlyrViewEventEmitter>(strongSelf->_eventEmitter);
+            RnVlcPlyrViewEventEmitter::OnError event{std::string([message UTF8String]), (int)code};
+            emitter->onError(event);
+          }
+        }];
+      }
+
     } else {
       UIView *fallbackView = [[UIView alloc] init];
       fallbackView.backgroundColor = [UIColor redColor];
diff --git a/node_modules/rn-vlc-plyr/ios/VlcPlyrView.swift b/node_modules/rn-vlc-plyr/ios/VlcPlyrView.swift
index f604afd..9c7ba11 100644
--- a/node_modules/rn-vlc-plyr/ios/VlcPlyrView.swift
+++ b/node_modules/rn-vlc-plyr/ios/VlcPlyrView.swift
@@ -2,14 +2,22 @@ import UIKit
 import MobileVLCKit
 
 @objc(VlcPlyrView)
-final class VlcPlyrView: UIView, VLCMediaPlayerDelegate {
+final class VlcPlyrView: UIView, @preconcurrency VLCMediaPlayerDelegate {
   
   private var player: VLCMediaPlayer?
   private var media: VLCMedia?
+  private var hasEmittedLoad = false
+  
+  // Callbacks to bridge to ObjC++/Fabric event emitter
+  @objc var onProgressCallback: ((_ currentTime: Double, _ duration: Double) -> Void)?
+  @objc var onLoadCallback: ((_ duration: Double, _ width: Double, _ height: Double) -> Void)?
+  @objc var onStateChangeCallback: ((_ state: String) -> Void)?
+  @objc var onErrorCallback: ((_ message: String, _ code: Int) -> Void)?
   
   @objc var url: String? {
     didSet {
       if oldValue != url {
+        hasEmittedLoad = false
         prepareMedia()
       }
     }
@@ -94,6 +102,64 @@ final class VlcPlyrView: UIView, VLCMediaPlayerDelegate {
     }
   }
   
+  // MARK: - VLCMediaPlayerDelegate
+  // These are called from VLC's internal thread, so we dispatch to main for safety.
+  
+  nonisolated func mediaPlayerTimeChanged(_ aNotification: Notification) {
+    DispatchQueue.main.async { [weak self] in
+      guard let self = self, let player = self.player else { return }
+      
+      let currentTimeMs = Double(player.time.intValue)
+      let durationMs = Double(player.media?.length.intValue ?? 0)
+      
+      // Convert to seconds for JS
+      let currentTimeSec = currentTimeMs / 1000.0
+      let durationSec = durationMs / 1000.0
+      
+      // Emit onLoad on first valid duration
+      if !self.hasEmittedLoad && durationSec > 0 {
+        self.hasEmittedLoad = true
+        self.onLoadCallback?(durationSec, 0, 0)
+      }
+      
+      // Emit onProgress
+      if durationSec > 0 {
+        self.onProgressCallback?(currentTimeSec, durationSec)
+      }
+    }
+  }
+  
+  nonisolated func mediaPlayerStateChanged(_ aNotification: Notification) {
+    DispatchQueue.main.async { [weak self] in
+      guard let self = self, let player = self.player else { return }
+      
+      let state: String
+      switch player.state {
+      case .buffering:
+        state = "buffering"
+      case .playing:
+        state = "playing"
+      case .paused:
+        state = "paused"
+      case .stopped:
+        state = "stopped"
+      case .ended:
+        state = "ended"
+      case .error:
+        state = "error"
+        self.onErrorCallback?("VLC playback error", -1)
+      case .opening:
+        state = "buffering"
+      default:
+        state = "idle"
+      }
+      
+      self.onStateChangeCallback?(state)
+    }
+  }
+  
+  // MARK: - Commands
+  
   @objc func play() {
     guard let player = player else {
       return
diff --git a/node_modules/rn-vlc-plyr/lib/module/RnVlcPlyrViewNativeComponent.ts b/node_modules/rn-vlc-plyr/lib/module/RnVlcPlyrViewNativeComponent.ts
index 293feaa..7c43977 100644
--- a/node_modules/rn-vlc-plyr/lib/module/RnVlcPlyrViewNativeComponent.ts
+++ b/node_modules/rn-vlc-plyr/lib/module/RnVlcPlyrViewNativeComponent.ts
@@ -1,8 +1,7 @@
 import { codegenNativeCommands, codegenNativeComponent } from 'react-native';
 import type * as React from 'react';
 import type { ViewProps, HostComponent } from 'react-native';
-// @ts-expect-error
-import type * as CodegenTypes from 'react-native/Libraries/Types/CodegenTypes';
+import type { DirectEventHandler, Double, Int32 } from 'react-native/Libraries/Types/CodegenTypes';
 
 export type PlayerStateType =
   | 'buffering'
@@ -20,22 +19,22 @@ export interface NativeProps extends ViewProps {
   loop?: boolean;
   muted?: boolean;
   // Events
-  onStateChange?: CodegenTypes.DirectEventHandler<{ state: string }>;
-  onProgress?: CodegenTypes.DirectEventHandler<{
-    currentTime: CodegenTypes.Double;
-    duration: CodegenTypes.Double;
+  onStateChange?: DirectEventHandler<{ state: string }>;
+  onProgress?: DirectEventHandler<{
+    currentTime: Double;
+    duration: Double;
   }>;
-  onLoad?: CodegenTypes.DirectEventHandler<{
-    duration: CodegenTypes.Double;
-    width: CodegenTypes.Double;
-    height: CodegenTypes.Double;
+  onLoad?: DirectEventHandler<{
+    duration: Double;
+    width: Double;
+    height: Double;
   }>;
-  onError?: CodegenTypes.DirectEventHandler<{
+  onError?: DirectEventHandler<{
     message: string;
-    code: CodegenTypes.Int32;
+    code: Int32;
   }>;
-  onVolumeChange?: CodegenTypes.DirectEventHandler<{
-    volume: CodegenTypes.Double;
+  onVolumeChange?: DirectEventHandler<{
+    volume: Double;
   }>;
 }
 
@@ -47,11 +46,11 @@ export interface NativeCommands {
   stop: (viewRef: React.ElementRef<RnVlcPlyrViewType>) => void;
   seek: (
     viewRef: React.ElementRef<RnVlcPlyrViewType>,
-    timeMs: CodegenTypes.Double
+    timeMs: Double
   ) => void;
   setVolume: (
     viewRef: React.ElementRef<RnVlcPlyrViewType>,
-    volume: CodegenTypes.Double
+    volume: Double
   ) => void;
 }
diff --git a/node_modules/rn-vlc-plyr/src/RnVlcPlyrViewNativeComponent.ts b/node_modules/rn-vlc-plyr/src/RnVlcPlyrViewNativeComponent.ts
index 293feaa..7c43977 100644
--- a/node_modules/rn-vlc-plyr/src/RnVlcPlyrViewNativeComponent.ts
+++ b/node_modules/rn-vlc-plyr/src/RnVlcPlyrViewNativeComponent.ts
@@ -1,8 +1,7 @@
 import { codegenNativeCommands, codegenNativeComponent } from 'react-native';
 import type * as React from 'react';
 import type { ViewProps, HostComponent } from 'react-native';
-// @ts-expect-error
-import type * as CodegenTypes from 'react-native/Libraries/Types/CodegenTypes';
+import type { DirectEventHandler, Double, Int32 } from 'react-native/Libraries/Types/CodegenTypes';
 
 export type PlayerStateType =
   | 'buffering'
@@ -20,22 +19,22 @@ export interface NativeProps extends ViewProps {
   loop?: boolean;
   muted?: boolean;
   // Events
-  onStateChange?: CodegenTypes.DirectEventHandler<{ state: string }>;
-  onProgress?: CodegenTypes.DirectEventHandler<{
-    currentTime: CodegenTypes.Double;
-    duration: CodegenTypes.Double;
+  onStateChange?: DirectEventHandler<{ state: string }>;
+  onProgress?: DirectEventHandler<{
+    currentTime: Double;
+    duration: Double;
   }>;
-  onLoad?: CodegenTypes.DirectEventHandler<{
-    duration: CodegenTypes.Double;
-    width: CodegenTypes.Double;
-    height: CodegenTypes.Double;
+  onLoad?: DirectEventHandler<{
+    duration: Double;
+    width: Double;
+    height: Double;
   }>;
-  onError?: CodegenTypes.DirectEventHandler<{
+  onError?: DirectEventHandler<{
     message: string;
-    code: CodegenTypes.Int32;
+    code: Int32;
   }>;
-  onVolumeChange?: CodegenTypes.DirectEventHandler<{
-    volume: CodegenTypes.Double;
+  onVolumeChange?: DirectEventHandler<{
+    volume: Double;
   }>;
 }
 
@@ -47,11 +46,11 @@ export interface NativeCommands {
   stop: (viewRef: React.ElementRef<RnVlcPlyrViewType>) => void;
   seek: (
     viewRef: React.ElementRef<RnVlcPlyrViewType>,
-    timeMs: CodegenTypes.Double
+    timeMs: Double
   ) => void;
   setVolume: (
     viewRef: React.ElementRef<RnVlcPlyrViewType>,
-    volume: CodegenTypes.Double
+    volume: Double
   ) => void;
 }
